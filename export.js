var exported = function(arr) {
  return 'There are ' + arr.length + ' items in this array';
};

module.exports = exported;

// module.exports = function(arr) {
//   return 'There are ' + arr.length + ' items in this array';
// };